<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<h3>Comments (<%= @article.comments.size %>)</h3>
<p><%= render partial: 'articles/comment', collection: @article.comments %></p>

<p><%= link_to "delete", article_path(@article), method: :delete, data: {confirm: "Really delete the article?"}%></p>
<p><%= link_to "Edit Page", edit_article_path(@article), class: "edit_article" %></p>

<%= render partial: 'comments/form' %>

<p><%= link_to "Home", articles_path %></p>

